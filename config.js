const config = {
  // 获取文本配置
  text: {
    number: 2 // 第几篇official，支持1-53
  },
  // 统计词频配置
  count: {
    witch: 'all'  // 统计哪一篇的词频，可选值为 1,2,3...,53,'all'
    // witch: 1
  }
}

module.exports = config
